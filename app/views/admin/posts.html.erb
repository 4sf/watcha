<main role="main" class="col-sm-9 ml-sm-auto col-md-10 pt-3">
  <h1>관리자 페이지</h1>
  <h2>게시글 현황</h2>
  <div class="table-responsive">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>#</th>
          <th>제목</th>
          <th>내용</th>
          <th>글쓴이</th>
          <th>그림</th>
          <th>삭제</th>
        </tr>
      </thead>
      <tbody>
        <% @posts.each do |post| %>
          <tr>
            <td><%= post.id %></td>
            <td><%= post.title %></td>
            <td><%= post.content %></td>
            <td><%= post.user.nickname %></td>
            <td><img src="<%= post.image.thumb %>"></td>
            <td>
              <%= link_to "삭제", "/admin/posts_destroy/#{post.id}", method: :delete, data: {confirm: "정말 지우시겠습니까?"} %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</main>
